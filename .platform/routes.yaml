"https://{default}/":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: false

"https://{default}/ws":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: false
